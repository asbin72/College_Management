# Railway Nixpacks Configuration for Kalpanaaa Education
[variables]
NODE_ENV = 'production'

[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node index.js'
